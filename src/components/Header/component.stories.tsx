import { Meta, StoryObj } from "@storybook/react";
import Header from ".";

export default {
  title: "Components/Header",
  component: Header,
  args: {},
  argTypes: {},
} as Meta;

export const Default: StoryObj = {};
